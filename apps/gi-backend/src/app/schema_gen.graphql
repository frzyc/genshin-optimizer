# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type GenshinUser {
  id: ID!
  uid: String!
}

type AddGenshinUserRes {
  success: Boolean!
  genshinUser: GenshinUser
  error: String
}

type CreateUserNameResponse {
  success: Boolean!
  error: String
}

type User {
  id: ID!
  username: String
  genshinUsers: [GenshinUser!]
}

type Artifact {
  id: ID!
  genshinUserId: String!
  setKey: String!
  slotKey: String!
  level: Int!
  rarity: Int!
  mainStatKey: String!
  location: String
  lock: Boolean!
  substats: [Substat!]!
}

type Substat {
  key: String!
  value: Float!
}

type Weapon {
  id: ID!
  genshinUserId: String!
  key: String!
  level: Int!
  ascension: Int!
  refinement: Int!
  location: String!
  lock: Boolean!
}

type AddWeaponRes {
  success: Boolean!
  weapon: Weapon
  error: String
}

type Talent {
  auto: Int!
  skill: Int!
  burst: Int!
}

type Character {
  id: ID!
  genshinUserId: String!
  key: String!
  level: Int!
  ascension: Int!
  constellation: Int!
  talent: Talent!
}

type AddCharacterRes {
  success: Boolean!
  character: Character
  error: String
}

type Query {
  getUserById(id: String!): User
  searchUsers(username: String!): [User!]!
  getGenshinUser(id: String!): GenshinUser
  getGenshinUserByUid(uid: String!): GenshinUser
  getArtifact(id: String!): Artifact
  getAllUserArtifact(genshinUserId: String!): [Artifact!]!
  getWeapon(id: String!): Weapon
  getAllUserWeapon(genshinUserId: String!): [Weapon!]!
  getCharacter(id: String!): Character
  getAllUserCharacter(genshinUserId: String!): [Character!]!
}

type Mutation {
  createUsername(username: String!): CreateUserNameResponse!
  addGenshinUser(uid: String!): AddGenshinUserRes!
  addArtifact(genshinUserId: String!, artifact: InputArtifact!): Artifact!
  updateArtifact(genshinUserId: String!, artifact: UpdateArtifact!): Artifact!
  addWeapon(genshinUserId: String!, weapon: InputWeapon!): AddWeaponRes!
  addCharacter(genshinUserId: String!, character: InputCharacter!): AddCharacterRes!
}

input InputArtifact {
  setKey: String!
  slotKey: String!
  level: Int!
  rarity: Int!
  mainStatKey: String!
  location: String
  lock: Boolean!
  substats: [InputSubstat!]!
}

input InputSubstat {
  key: String!
  value: Float!
}

input UpdateArtifact {
  setKey: String
  slotKey: String
  level: Int
  rarity: Int
  mainStatKey: String
  location: String
  lock: Boolean
  substats: [InputSubstat!]
  id: ID!
}

input InputWeapon {
  key: String!
  level: Int!
  ascension: Int!
  refinement: Int!
  location: String!
  lock: Boolean!
}

input InputCharacter {
  key: String!
  level: Int!
  ascension: Int!
  constellation: Int!
  talent: InputTalent!
}

input InputTalent {
  auto: Int!
  skill: Int!
  burst: Int!
}