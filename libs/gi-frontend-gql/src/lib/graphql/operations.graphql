mutation createUsername($username: String!) {
  createUsername(username: $username) {
    success
    error
  }
}

query getUser($dbId: String!) {
  getUserById(id: $dbId) {
    id
    username
  }
}
