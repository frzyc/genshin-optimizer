{
  "val": 200,
  "meta": {
    "formula": "[200] gather 2 node(s) matching { #heal 0 (0) PrototypeAmber team formula.heal [sum] } (for { #heal PrototypeAmber team formula.heal })",
    "deps": [
      {
        "readSeq": "{ team } <- { #heal 0 (0) PrototypeAmber team formula.heal }",
        "formula": "[100] gather 1 node(s) matching { #heal 0 PrototypeAmber own formula.heal [unique] } (for { 0 !dst own })",
        "deps": [
          {
            "readSeq": "{ own formula.heal } <- { #heal 0 PrototypeAmber own formula.heal }",
            "formula": "[100] 100"
          }
        ]
      },
      {
        "readSeq": "{ team } <- { #heal 0 (0) PrototypeAmber team formula.heal }",
        "formula": "[100] gather 1 node(s) matching { #heal 1 PrototypeAmber own formula.heal [unique] } (for { 1 !dst own })",
        "deps": [
          {
            "readSeq": "{ own formula.heal } <- { #heal 1 PrototypeAmber own formula.heal }",
            "formula": "[100] 100"
          }
        ]
      }
    ]
  }
}