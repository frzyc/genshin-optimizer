/** Visible edge for Pando opt panels. `CardThemed` has no border; GI body is a gradient near `contentNormal`. */
export const pandoCardSx = {
  border: '1px solid',
  borderColor: 'contentNormal.light',
} as const
