# biome

This library was generated with [Nx](https://nx.dev).


This auto-injects a `lint` and `format` task into every project, using `biome`.

## Building

Run `nx build biome` to build the library.
