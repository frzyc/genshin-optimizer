name: Deploy PR

on:
  pull_request:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: ${{ steps.branch-deploy.outputs.ref }}
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'yarn'
      # Find the PR associated with this push, if there is one.
      - uses: jwalton/gh-find-current-pr@v1
        id: findPr
        with:
          state: open
      - name: Yarn install
        run: |
          yarn install --immutable --immutable-cache
      - name: Setup .env
        run: |
          printf '%s' "$ENVFILE" > apps/frontend/.env.local
        env:
          ENVFILE: ${{ secrets.ENVFILE }}
      - name: Build genshin-optimizer
        run: |
          NX_URL_GITHUB_GO_CURRENT_VERSION="https://github.com/${{ github.repository }}/commit/$(git rev-parse HEAD)" \
          yarn run nx run frontend:build:production --base-href=""
      - name: Checkout gh-pages
        uses: actions/checkout@v3
        with:
          ref: gh-pages
          path: gh-pages
      - name: Move to gh-pages
        run: |
          mkdir --parents "gh-pages/pr${{ steps.findPr.outputs.pr }}"
          rm -rfv "gh-pages/pr${{ steps.findPr.outputs.pr }}" || true
          mv "dist/apps/frontend" "gh-pages/pr${{ steps.findPr.outputs.pr }}"
      - name: Make commit to deploy
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ./pr${{ steps.findPr.outputs.pr }}
          git commit -m "build pr${{ steps.findPr.outputs.pr }} ${{ steps.branch-deploy.outputs.ref }}"
          git push origin gh-pages
        working-directory: gh-pages
      - name: Update deployment message
        run: |
          echo "DEPLOY_MESSAGE=Deployed to https://${{ github.repository_owner }}.github.io/${GITHUB_REPOSITORY#*/}/pr${{ steps.findPr.outputs.pr }}" >> $GITHUB_ENV
